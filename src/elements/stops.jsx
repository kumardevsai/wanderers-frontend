import styled from 'styled-components';

import { colors, fontSizes, padding } from '../elements/variables';

const Stop = styled.li`
  margin: 0 auto;
  padding: ${padding.ultramini};
  font-size: ${fontSizes.small};
`;
export { Stop };
